input {

  beats {

    port => 5044
  }
}

filter {

  mutate {

    add_field => {

      "security_platform" => "SIEM"
    }
  }
}

output {

  elasticsearch {

    hosts => ["http://elasticsearch:9200"]

    index => "siem-events-%{+YYYY.MM.dd}"
  }

  stdout {}
}
