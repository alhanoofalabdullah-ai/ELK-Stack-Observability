input {

  beats {

    port => 5044
  }
}

filter {

  mutate {

    add_field => {

      "platform" => "DevOps"
    }
  }
}

output {

  elasticsearch {

    hosts => ["http://elasticsearch:9200"]

    index => "devops-events-%{+YYYY.MM.dd}"
  }

  stdout {}
}
